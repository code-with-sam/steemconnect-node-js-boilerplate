extends layout

block content
  include partials/nav

  main.profile(data-username=`${username}` data-user=`${user}` data-feed-type='user-feed')
    include partials/profile-nav
    section.profile-feed
      .row.feed-insert
      .btn.btn-primary.load-more-posts Load More Posts
